// Central sound hooks – currently stubs, real audio to be plugged in later.

export const AUDIO_EVENTS = {
    UNIT_SELECTED: 'unit_selected',
    // later: UNIT_CONFIRMED_MOVE, UNIT_ATTACK, BUILD_COMPLETE, etc.
};

export function playAudio(eventName, payload = {}) {

}